# Dependencies for scripts/generate_ontology_diagram.py
# (pure-Python diagram render — no SVG rasteriser / Graphviz / browser needed)
rdflib>=7.0
Pillow>=9.2

# Dependencies for scripts/validate.py (SHACL gate over shapes/takt-shapes.ttl)
pyshacl>=0.30
